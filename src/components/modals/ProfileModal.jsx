import SettingsModal from './SettingsModal';

/**
 * Backwards compatibility alias:
 * Re-exports SettingsModal to ensure any legacy imports or lazy-loaded routes
 * resolve transparently to the unified Settings & Profile Hub without duplicate rendering.
 */
export default SettingsModal;
